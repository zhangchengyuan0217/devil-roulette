---
description: Devil Roulette 对局单屏布局硬性规则（100dvh、手牌完整、悬停放大、工具栏可见）
globs: css/style.css,index.html,**/layout.md
alwaysApply: false
---

# 对局界面 · 单屏布局规则

改 `css/style.css` / `index.html` 对局 UI 时必须遵守。完整说明见同目录 `layout.md`。

## 硬性目标

1. **单屏锁定**：`html, body` 为 `100vw × 100dvh`，对局中 `overflow: hidden`，**禁止页面滚动条**。
2. **手牌完整显示**：道具手牌静止态必须整张可见，不得被视口底边 / 工具栏 / 父容器裁切。
3. **悬停向上放大**：手牌悬停约 **≥2.7×**（`--hand-hover-scale`），方向向上，不得被牌桌挡住。
4. **底部工具栏固定可见**：`.actions-dock`（使用道具等）必须完整在视口内。

`body.phase-ended` 可临时允许滚动。

## 结构

```
body (100dvh, flex column, overflow hidden)
├── .topbar                 /* flex: 0 0 auto */
└── .table-wrap.active      /* flex: 1; min-height: 0; grid */
    ├── .round-banner       /* auto */
    ├── .board-arena        /* minmax(0, 1fr) —— 唯一可压缩区 */
    └── .bottom-panel       /* 0 0 auto —— 按内容撑开，不可压扁 */
        ├── .me-zone
        └── .actions-dock
```

## 禁止事项

- ❌ 给 `.bottom-panel` 过小的 `max-height` 压扁手牌/工具栏
- ❌ `.hand-fan` 用大额负 `margin-top` 把静止手牌推出视口
- ❌ 压缩 `.me-zone` / `.actions-dock`（须 `flex: 0 0 auto`）来「省高度」
- ❌ 让顶座位（红队）横向拉满；宽度应与左右蓝队座位列一致
- ❌ 左右座位把「开枪」放在角色牌正下方且整体居中（矮屏时会压到分界线以下被底栏挡住）

## 正确做法

- ✅ 只让 `.board-arena` 吸收剩余高度
- ✅ `.bottom-panel` 的 `z-index` **高于** `.board-arena`（手牌悬停盖住牌桌）；左右「开枪」用横向排布留在牌桌区内，禁止伸入底栏
- ✅ 手牌悬停：`transform-origin: center bottom` + `translateY` 向上 + `scale(var(--hand-hover-scale))`，手牌区单独更高 `z-index`
- ✅ 改完后自测：1280×720 / 1920×1080 无滚动条；手牌四角可见；悬停 ≥2.7×；「使用道具」可点；四座位「开枪」完整可点
