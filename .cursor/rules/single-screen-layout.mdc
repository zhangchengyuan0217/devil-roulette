---
description: Devil Roulette 对局单屏布局（100dvh、手牌、预览、仅手机横屏适配）
globs: css/style.css,index.html,**/layout.md
alwaysApply: false
---

# 对局界面 · 单屏布局规则

改 `css/style.css` / `index.html` 对局 UI 时必须遵守。完整说明见同目录 `layout.md`。

## 硬性目标

1. **单屏锁定**：`100dvh` + `overflow: hidden`，对局禁止页面滚动条。
2. **手牌完整显示**：静止态整张可见。
3. **预览**：桌面悬停轻抬 + `.hand-preview`；手机横屏点选 + fixed 居中。**竖屏不做特殊适配**。
4. **`.actions-dock` 始终完整可见**。

## 禁止事项

- ❌ 压扁 `.bottom-panel` / `.me-zone` / `.actions-dock` 来「省高度」
- ❌ 对手牌本身做 ≥2× 缩放当预览
- ❌ 为竖屏手机单独改对局十字布局（只需横屏）

## 正确做法

- ✅ 只让 `.board-arena` 吸收剩余高度
- ✅ 手机适配仅用：`(orientation: landscape) and (max-height: 560px) and (max-width: 960px)`
- ✅ 横屏：底栏单行、迷你座位、预览 fixed、关 hover 放大
- ✅ 自测：桌面 + 手机横屏（左右座位完整可见，底栏不竖堆）
